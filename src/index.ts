import { strom } from "./functions";
export * from "./utils";
export const {
    accumulator,
    accumulatorBy,
    batch,
    child,
    collect,
    compose,
    concat,
    demux,
    duplex,
    filter,
    flatMap,
    fromArray,
    join,
    last,
    map,
    merge,
    parallelMap,
    parse,
    rate,
    reduce,
    replace,
    split,
    stringify,
    tap,
    unbatch,
    instance,
} = strom();
